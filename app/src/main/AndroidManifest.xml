<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.LeaderboardActivity"></activity>
        <activity android:name=".Controller.EditNameActivity" />
        <activity android:name=".Controller.ShopActivity" />
        <activity android:name=".Controller.GameActivity" />
        <activity android:name=".Controller.LogInActivity" />
        <activity android:name=".Controller.SignUpActivity" />
        <activity android:name=".Controller.GameResultsActivity" />
        <activity android:name=".Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.SettingsActivity" />
        <activity android:name=".Controller.HowToPlayActivity" />
    </application>

</manifest>